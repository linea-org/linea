# Own external Approval Requests by subject rather than session

Any current End-User Session for the same External Subject and Application may decide that subject's Approval Request; the initiating browser session is recorded for audit but is not the authorization owner. Treating the person as the approver allows reloads, session replacement, and cross-device decisions without weakening subject isolation, while binding ownership to one short-lived session would strand valid requests whenever that session ended.
